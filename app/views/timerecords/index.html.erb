<div class="timerecords">
<h1> Hours from <%= params[:weekstart] %> to <%= params[:weekend] %> for <%= @user.firstname %> <%= @user.lastname%>
	<small> 
		
	</small>
</h1>
<div>
<%= form_tag nil, :method => :get, :class => 'navbar-form navbar-left' do %>
			<div class="form-group">
			<%=date_field_tag 'weekstart', $beginning_of_last_week, :class => "form-control"%>
			<%=date_field_tag 'weekend', $end_of_last_week, :class => "form-control"%>
      		<%=hidden_field_tag 'selecteduser', '', value: params[:selecteduser]%>
			</div>
			<button type="submit" class="btn btn-default">Submit</button>
<% end %>
<span>
    
</span>
</div>
<div class="table-responsive">


<table class="table table-hover">
  <tr>
  	<th></th>
    <th>Job Number</th>
    <th>Date</th>       
    <th>Time In</th>
    <th>Time Out</th>
    <th>Total</th>
  </tr>
  <%= render @timerecords %>
  <tr>
  	<td></td>
    <td></td> 
    <td></td>
    <td></td>
    <td></td>
    <td><%= @timerecords.to_a.sum(&:timeTotal).round(2)%> Hours</td>
 </tr>
 </table>
</div>
</div>   

</div>